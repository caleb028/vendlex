import BusinessProfilePage from "@/app/businesses/[slug]/page";

// Export standard dynamic metadata and delegate directly to the full-featured storefront component
export default BusinessProfilePage;
